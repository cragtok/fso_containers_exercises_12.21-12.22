{
  "typescript": true,
  "removeViewBox": false,
  "replaceAttrValues": {
    "#1C202A": "{props.color || 'currentColor'}",
    "currentColor": "{props.color || 'currentColor'}"
  },
  "svgProps": {
    "width": "{props.width || 24}",
    "height": "{props.height || 24}"
  }
}
